import 'class/Person.dart';

main() {
  // Person p;
  // p?.printInfo();

  // var p1;
  // p1 = '';
  // p1 = new Person('张三', 4);
  // (p1 as Person).printInfo();

  // Person p1 = new Person('张三', 4);
  // p1.name = '李四';
  // p1.age = 40;
  // p1.printInfo();

//   Person p1 = new Person('张三', 4);
//   p1
//     ..name = '老李'
//     ..age = 30
//     ..printInfo();
}

/*
  ? 条件运算符
  as 类型转换
  is 类型判断
  .. 联级操作
  
  */
